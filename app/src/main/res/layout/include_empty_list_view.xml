<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/empty_list_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerInParent="true"
    android:visibility="gone">

    <!-- Empty List Icon -->
    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/empty_list_iv_icon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerInParent="true"
        android:layout_above="@+id/list_tv_msg"
        android:layout_centerHorizontal="true"
        android:tint="@color/grey_400"
        app:srcCompat="@drawable/baseline_cloud_queue_24" />

    <!-- Empty List TextView -->
    <android.support.v7.widget.AppCompatTextView
        android:id="@+id/list_tv_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="10dp"
        android:text="@string/no_item_to_show"
        android:textColor="@color/grey_400"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- Empty List ImageButton -->
    <android.support.v7.widget.AppCompatImageButton
        android:id="@+id/empty_list_ibt_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/list_tv_msg"
        android:layout_centerHorizontal="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:tint="@color/colorPrimary"
        app:srcCompat="@drawable/baseline_refresh_black_48" />

</RelativeLayout>